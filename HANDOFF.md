# 项目交接记录

日期：2026-06-21

## 1. 当前仓库状态

- 工作目录：`E:\Codex\children`
- 当前分支：`develop`
- 当前工作区：干净，无未提交代码
- 最新提交：`7aec558 Use difficulty buttons and concise answer labels`
- `master` 保留较早稳定 UI demo，`develop` 是当前开发分支

## 2. 已完成的旧项目：AI 学习卡片生成器

已经实现一个手机网页 demo：

- 首页：科目、年级、难度按钮、学习材料输入框、语音/拍照占位按钮、生成按钮
- AI 生成接口：`/api/generate-cards`
- 本地开发服务：`npm.cmd run dev` 后访问 `http://localhost:3000`
- 支持 `.env.local` 配置：
  - `OPENAI_API_KEY`
  - `OPENAI_MODEL`
  - `OPENAI_BASE_URL`
- 支持中转 API，代码会请求 `${OPENAI_BASE_URL}/responses`
- 无 Key 或 AI 失败时前端会 fallback 到演示卡片
- 自测流程：
  - 查看答案
  - 点击“会 / 不会”
  - 总结页统计已掌握和不会
  - 支持只练不会的卡片
- UI 预览模式：`http://localhost:3000?demo=1`

注意：
- 浏览器直接打开 `file:///E:/Codex/children/index.html` 只能看静态页面，不能真实调用本地 API。
- 真实测试必须打开 `http://localhost:3000`。

## 3. 今天的重要转向

用户认为“AI 学习卡片生成器”偏学习工具，不够好玩，不适合作为社区面向孩子的 AI 活动主项目。

新方向改为：

**AI 互动故事冒险机**

定位：
- 孩子扫码打开手机网页
- 用文字或语音输入故事开头
- AI 生成故事第一幕
- 每一幕给 2-3 个剧情选择
- 孩子通过选择推动故事发展
- 最后生成故事结局和故事作品卡
- 作品卡包含标题、主角、摘要、关键选择、结局类型和封面插图

产品倾向：
- 做手机网页，不做原生 APP
- 活动现场扫码即用
- 第一版先做文字输入，语音入口可以规划但不一定先实现
- 文本故事可接 DeepSeek 或兼容 OpenAI 接口的中转模型
- 图片生成需要单独图片模型/API
- 第一版建议只生成最终封面图，不做每一幕漫画

## 4. 已确认的新项目用户旅程地图

用户已确认以下旅程地图：

1. 进入故事机  
   孩子扫码打开网页，看到一个像故事游戏的首页。

2. 输入故事开头  
   孩子输入文字，或点击语音入口说出故事开头。第一版优先文字输入。

3. AI 生成故事第一幕  
   系统基于开头生成第一段故事，包括主角、场景、冲突或悬念。

4. 选择故事走向  
   每一幕结尾给出 2-3 个选择，孩子点击其中一个决定下一步。

5. 持续互动接龙  
   系统记住前文剧情和孩子选择，继续生成下一幕，避免前后矛盾。第一版建议限制 4-6 幕。

6. 生成结局  
   达到设定幕数后，AI 根据已有剧情生成完整结局，并总结标题、主角、关键选择、结局类型。

7. 生成故事卡  
   系统生成故事作品卡，包含故事标题、主角、简短摘要、关键选择、结局类型和一张封面插图。

## 5. PRD 技能流程进度

使用技能：`prd-doc-writer`

技能要求：
- 不能直接生成终稿 PRD
- 先确认用户旅程地图
- 再补 Mermaid 用户操作流程图
- 然后逐个用户故事讨论
- 每个涉及 UI 的故事都要画 ASCII 线框图
- 每个故事都要写 AC 验收标准
- 所有故事确认完后，用户明确说“可以生成了”，再生成完整 PRD

当前进度：
- 用户旅程地图已确认
- 已向用户解释技能中的 AC、ASCII Wireframe、Mermaid 的作用
- 下一步应该补 Mermaid 用户操作流程图

## 6. 明天建议继续的步骤

### 第一步：继续 PRD 流程

先输出 AI 互动故事冒险机的 Mermaid 用户操作流程图，请用户确认。

建议流程图覆盖：
- 进入首页
- 输入故事开头
- 判断输入是否有效
- AI 生成第一幕
- 展示 2-3 个选择
- 孩子选择
- AI 续写下一幕
- 判断是否达到结局轮数
- 生成结局
- 生成故事卡
- 图片生成成功/失败分支

### 第二步：进入 US-01

建议第一个用户故事：

`US-01: 作为孩子，我希望扫码进入故事机并输入一个故事开头，以便开始一次属于自己的 AI 故事冒险。`

需要讨论：
- 首页风格：游戏感、故事感，不像学习工具
- 输入方式：文字输入优先，语音按钮保留还是第一版实现
- 开头最短字数
- 是否提供开头示例
- 是否提供故事类型选择，例如奇幻、校园、科幻、侦探、搞笑
- UI 线框图
- AC 验收标准

### 第三步：规划旧项目归档

用户要求旧项目不要和新项目混在一起。

注意用户的 AGENTS 指令：禁止批量删除文件或目录。

建议做法：
- 不删除任何文件
- 新建归档目录，例如 `archive/study-card-generator/`
- 将旧项目文件移入该目录前，先向用户确认
- 或者更稳妥：保持当前目录不动，新项目新建在 `story-adventure/` 目录

推荐明天先问用户：
- 是把旧项目移动到归档目录，还是直接在新目录创建新项目？

## 7. 风险与待确认

- 图片生成接口尚未选择，DeepSeek/文本模型不能直接生成图片。
- 语音输入是否第一版实现未确认。
- 是否需要保存故事历史未确认。
- 故事是否多人共玩还是单人轮流玩未确认。
- 作品卡是否需要下载图片/分享二维码未确认。
- 部署平台未确认，建议先网页本地开发，再部署到 Vercel/Netlify/Cloudflare Pages。

## 8. 明天接续提示词

可以直接对 Codex 说：

“继续昨天的 AI 互动故事冒险机 PRD，先补 Mermaid 用户操作流程图，然后进入 US-01。”

